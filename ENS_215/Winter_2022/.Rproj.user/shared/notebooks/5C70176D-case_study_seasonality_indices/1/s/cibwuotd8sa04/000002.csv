"0","```r
df_monthly_normals <- precip_data %>% 
  filter(Year >= 1980) %>% 
  
  group_by(state_cd, Month) %>% 
  summarize(observation_value = mean(Precip_inches)) %>% 
  
  rename(group_name = state_cd)
```"
"1","`summarise()` regrouping output by 'state_cd' (override with `.groups` argument)
"
